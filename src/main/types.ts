export type CollectionElement = [string];
export type DirectoryList = {
    clips: string,
    subtitles: string,
    thumbnails: string,
    previewImage: string
}

export type ClipPaths = {
    clip: string,
    subtitle: string,
    thumbnail: string
}