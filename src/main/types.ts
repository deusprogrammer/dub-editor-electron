export type CollectionElement = [string];
export type DirectoryList = {
    clips: string,
    subtitles: string,
    thumbnails: string,
    previewImage: string,
    collectionMeta: string,
    batchCacheMeta: string,
    batchVideoMeta: string
}

export type ClipPaths = {
    clip: string,
    subtitle: string,
    thumbnail: string
}